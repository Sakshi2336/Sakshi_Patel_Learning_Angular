import { CricketerRolePipe } from './cricketer-role.pipe';

describe('CricketerRolePipe', () => {
  it('create an instance', () => {
    const pipe = new CricketerRolePipe();
    expect(pipe).toBeTruthy();
  });
});
