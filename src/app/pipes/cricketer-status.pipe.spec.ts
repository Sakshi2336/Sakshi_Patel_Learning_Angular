import { CricketerStatusPipe } from './cricketer-status.pipe';

describe('CricketerStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CricketerStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
